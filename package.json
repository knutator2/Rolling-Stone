{
  "name": "Rolling-Stone",
  "version": "0.0.1",
  "description": "Wahrscheinlich hast Du auch schon mal einen Stein gefunden und Dich über seine Beschaffenheit, Farbe oder Form gewundert. Wusstest Du, dass dieses Gesteinsmaterial mehrere Millionen Jahre alt ist und in den Eiszeiten von Gletschern aus Skandinavien nach Berlin und Brandenburg transportiert wurde?",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -rf build && mkdir build",
    "build": "npm run build:browserify && npm run build:sass && npm run build:copy",
    "build:browserify": "browserify js/app.js --outfile js/bundle.js",
    "build:sass": "sass --compass styles/main.scss:styles/main.css",
    "build:copy": "cp index.html build/index.html && cp -r img build/img && cp -r js build/js && cp -r node_modules build/node_modules && cp -r styles build/styles",
    "dev": "npm run sass-watch & npm run local-server",
    "sass-watch": "sass --compass --watch styles/main.scss:styles/main.css",
    "local-server": "http-server ."
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/knutator2/Rolling-Stone.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/knutator2/Rolling-Stone/issues"
  },
  "homepage": "https://github.com/knutator2/Rolling-Stone",
  "dependencies": {
    "angular": "^1.4.7",
    "angular-animate": "^1.4.7",
    "angular-leaflet-directive": "^0.8.8",
    "angular-resource": "^1.4.7",
    "angular-route": "^1.4.7",
    "angular-simple-logger": "^0.1.4",
    "d3": "^3.5.6",
    "jquery": "^2.1.4",
    "leaflet": "^0.7.5",
    "leaflet-easybutton": "^1.1.2",
    "leaflet.fullscreen": "^1.3.0",
    "modernizr": "^3.1.0",
    "underscore": "^1.8.3"
  },
  "devDependencies": {
    "browserify": "^13.0.0",
    "http-server": "^0.8.5"
  }
}
